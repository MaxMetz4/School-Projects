def countIf(f, L):
    return len(list(filter(f,L)))
    
def myFilter(f,L):
    baseVal = 0
    else:
        def recursiveStep(head, tail):
            pass
    return baseVal if len(L) == 0 \
           else recursiveStep(L[0], L[1:])

def myFilter2(f, L):
    if L == []:
        return L
    elif f(L[0]):
        return 
